import 'package:flutter/material.dart';

class CallCancelledScreen extends StatelessWidget {
  const CallCancelledScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Call canceled"),),
    );
  }
}
